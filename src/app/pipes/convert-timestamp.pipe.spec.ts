import { ConvertTimestampPipe } from './convert-timestamp.pipe';

describe('ConvertTimestampPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
